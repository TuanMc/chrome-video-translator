@AGENTS.md

## Claude Code

- For architecture/API/dependency questions, read `aidlc-docs/inception/reverse-engineering/` before answering from assumption — it was generated by reading the actual code, not summarized from memory.
- Prefer plan mode for changes touching `faster_whisper.py`, `translation_socket.py`, or anything else duplicated between `nllb-server` and `libre-server` — confirm the sync-to-both-servers scope before writing code.
