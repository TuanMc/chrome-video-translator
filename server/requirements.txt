fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
faster-whisper>=1.2,<1.3
numpy>=2.0,<3.0

# NLLB translation (POC4). torch is installed from the CPU-only wheel index —
# see server/README.md for the install command; a plain `pip install torch`
# here would pull the much larger CUDA-bundled build.
transformers>=4.57,<4.58
sentencepiece>=0.2,<0.3
