# Copy this file to .env and fill in your real key — .env is gitignored,
# .env.example is not (it's just a template, no real secret in it).
# Get a key at https://soniox.com/.
SONIOX_API_KEY=your-key-here
